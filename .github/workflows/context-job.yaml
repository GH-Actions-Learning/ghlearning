name: Job context variable

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: pwd

      - name: Send failure notification
        if: ${{ always() && job.status == 'failure' }}
        run: echo "Job failed! Sending notification..."

