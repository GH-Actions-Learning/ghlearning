jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: |
          exit 1

      - name: Cleanup on failure
        if: ${{ always() && job.status == 'failure' }}
        run: pwd
