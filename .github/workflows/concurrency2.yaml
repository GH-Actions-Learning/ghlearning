name: Build and Deploy

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🏗️ Building..."

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "🚀 Deploying..."
