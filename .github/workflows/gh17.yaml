name: ghw-workflow-dispatch2
on:
    workflow_dispatch:
      inputs:
        testing:
          description: "this for testing"
          requried: true
          type: choice
          default: "30lpa"
          options: 
            - 30lpa
            - big-house
            - plots

jobs:
  select-option:
    runs-on: ubuntu-latest
    steps:
      - name: testing
        run: echo "ok"
          
