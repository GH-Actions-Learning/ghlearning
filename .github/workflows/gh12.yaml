name: path-filter1

on:
  push:
    path:
      - src/*

jobs:
  changein-src-folder:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: echo "trigger only when files are changed in src dir"